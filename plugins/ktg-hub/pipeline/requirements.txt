# KTG Content Pipeline Dependencies
# Install: pip install -r pipeline/requirements.txt
#
# Runtime deps only — these are what ktg_pipeline actually imports:
#   pyyaml   -> config + frontmatter parsing (import yaml)
#   requests -> provider HTTP calls (Gemini / Ollama / OpenRouter / LM Studio)

pyyaml>=6.0
requests>=2.28.0
