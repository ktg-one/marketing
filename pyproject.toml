[project]
name = "ktg-one"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kimi-cli>=1.44.0",
    "numpy>=2.4.6",
    "psutil>=7.2.2",
    "tool>=0.8.0",
]
